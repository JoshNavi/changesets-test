on:
  release:
    types: [released]

jobs:
  generate-changelog:
    runs-on: ubuntu-latest
    steps:
      - run: echo "$TAG_NAME"
        env:
          TAG_NAME: ${{github.event.release.tag_name}}
      - run: echo "$BODY"
        env:
          BODY: ${{github.event.release.body}}
